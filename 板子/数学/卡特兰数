#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MAXN = 20;
ll f[MAXN];
int main() {
	f[0] = f[1] = 1;
	int n;
	cin >> n;
	for (int i = 2; i <= n; i++) {
		f[i] += f[i - 1] * (i*4 - 2) / (i + 1);
	}
	cout << f[n];
	return 0;
}

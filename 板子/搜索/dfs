int dfs(int t) //参数不一定只有1个
{
    if(满足输出条件) {
        输出解 || 计数;
        return;
    }

    for(int i=1;i<=尝试方法数;i++) {
        if(满足进一步搜索条件) {
              为进一步搜索所需要的状态打上标记;
              dfs(t+1);
              回溯;
        }
    }
}
